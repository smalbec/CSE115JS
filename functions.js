function some_math(x,y){
    return x + y;
}

console.log(some_math(2,4));



function power(x,y){;{
    var software = Math.pow(entertainment, cake);

     return (software);
}

function assign(x) {
    return x + " " + "implication"
}

function download_time(file_gb){
        var download_speed = 0.00175 //mbps to GB
        var download = file_gb / download_speed;
        return download
    }

    function line_eval(x){
        var y = 1.58 * x + 4.87
        return y
    }


    function parabola_eval(x){
        return ((1.58*Math.pow(x,2)) + (-9.62 * x) + 7.56 )
    }


    function pool_volume(x){
        radius = 15;
        volume = (Math.PI * (Math.pow(15,2)) * x);
        return volume;
    }

    function replacement(x){
        var y = x.replace(/q/g,"d") ;
        return y;
    }


    function absolute(x){
        return Math.abs(x);
    }

    function distance(x,y){
        euclidian = Math.sqrt((Math.pow((-8.5 - x), 2 )) + Math.pow((7.0 - y),2));
        return euclidian;
    }

    function tweets(x){
        var n_tweets = Math.ceil(x.length / 280)
        return n_tweets
    }